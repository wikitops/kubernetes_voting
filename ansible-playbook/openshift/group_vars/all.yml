# Generated by Terraform

# default information about the provider and environment
cloud_provider_region: "ca-central-1"
cloud_provider_name: "Amazon AWS"
cloud_provider_shortname: "AWS"
environment_name: "Proof Of Concept"
environment_shortname: "POC"

# Valid bootstrap options (required): ubuntu, coreos, centos, none
bootstrap_os: "centos"
ansible_user: "centos"

# Use proxycommand if bastion host is in group all
# This change obseletes editing ansible.cfg file depending on bastion existance
ansible_ssh_common_args: "{% if 'bastion0.ca.treeptik.local' in groups['all'] %} -o ProxyCommand=\"ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -W %h:%p {{ ansible_user }}@35.183.5.220 \" {% endif %}"
